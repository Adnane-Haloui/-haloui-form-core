import  React from 'react';
import  * as ReactDOM from 'react-dom'
//import App from './app'
/*
const param : ParamsType = {
    rough :  {
        edit : 'hello',
    },
    draft : {
        edit : 'hi'
    }
}


interface Humain {
    readonly name: string,
    age : number,
    check():string | number

}

class Man implements Humain {
    readonly name: string
    age: number
    title = 'hello im man'
       constructor(name :string , age : number) {
        this.name = name;
        this.age = age
       }



    check(): string|number {
       return this.name
    }
}

const data : Humain = new Man('adnane', 14)

// ?? not null && undefined
// const result = data ?? 'default_value"
// check if exisiting pro find?.id?.edit


// keyof i want a key of object


function handle<T extends object , U extends keyof T>(obj : T , key : U){
    return  obj[key]
}

console.log(handle({name : 'hello'} , 'name'))


const handleArrow = <T extends object , U extends keyof T & string> ( obj: T , key : U) : string => {
    return 'hello'
}

type Objectable = {
    name : string
}
// const internal = handleArrow<Objectable,string >({name : 'hello'} , 'name')

//console.log(JSXEL)
*/
/*

console.log(App)

ReactDOM.render(  React.createElement(App) , document.querySelector('#root'))
*/
